name: Build and Publish Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build standalone HTML
        run: npm run build

      - name: Prepare Release Asset
        run: cp dist/index.html SuperluminalColorMixer.html

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: SuperluminalColorMixer ${{ github.ref_name }}
          files: SuperluminalColorMixer.html
          generate_release_notes: true
          draft: false
          prerelease: false
